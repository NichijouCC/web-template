version: '3.0'
services:
  web:
    build: .
    container_name: web_template
    restart: always
    expose: 
      - 80
#     networks:
#       - production_net
# networks:
#   production_net:
#     external: true