/**
 * (可选) 在内部配置ProjectConfig
 */
export const private_config: IProjectConfig = {
    app_config: {
        common: {
            "domain": "target_domain"
        },
        "dev": {},
        "test": {},
        "prod": {}
    }
}